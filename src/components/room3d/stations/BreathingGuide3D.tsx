'use client';

import StationPlaceholder3D from './StationPlaceholder3D';

export default function BreathingGuide3D({ isActive }: { isActive?: boolean }) {
  return <StationPlaceholder3D color="#8abfff" label="Breathing Guide" isActive={isActive} />;
}
